name: Frontend Tests

on:
  push:
    paths:
      - 'frontend/**'
      - 'lib/**'
  pull_request:
    branches: [master]

jobs:
  frontend-test:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (Backend for lib build)
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.4'
          cache: 'npm'

      - name: Install backend dependencies (for lib build)
        run: npm ci

      - name: Build shared library
        run: npm run build:lib

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci

      - name: Run frontend type-check
        run: |
          cd frontend
          npx tsc --noEmit

      - name: Run frontend linting
        run: |
          cd frontend
          npm run lint

      - name: Run frontend tests
        run: |
          cd frontend
          npm run ci:karma

      - name: Build frontend
        run: |
          cd frontend
          npm run build

      - name: Upload frontend build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist/
          retention-days: 30

  # Parallel runner being validated as a replacement for Karma (jsdom, no real
  # browser — see spec/FRONTEND_TEST_RUNNER.md). Currently runs the migrated
  # subset; the include scope grows as specs are ported off Jasmine/Karma.
  frontend-test-vitest:
    runs-on: blacksmith-4vcpu-ubuntu-2404

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js (Backend for lib build)
        uses: actions/setup-node@v4
        with:
          node-version: '20.19.4'
          cache: 'npm'

      - name: Install backend dependencies (for lib build)
        run: npm ci

      - name: Build shared library
        run: npm run build:lib

      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci

      - name: Run frontend tests (Vitest / jsdom)
        run: |
          cd frontend
          npm run test:vitest